{
  "name": "consumer-4",
  "private": true,
  "scripts": {
    "test": "npm i ../dependency-bar ../dependency-foo && conflicting-command",
    "reset": "rm package-lock.json && rm -rf node_modules"
  }
}
