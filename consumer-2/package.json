{
  "name": "consumer-2",
  "private": true,
  "dependencies": {
    "dependency-bar": "file:../dependency-bar",
    "dependency-foo": "file:../dependency-foo"
  },
  "scripts": {
    "test": "npm i && conflicting-command",
    "reset": "rm package-lock.json && rm -rf node_modules"
  }
}
